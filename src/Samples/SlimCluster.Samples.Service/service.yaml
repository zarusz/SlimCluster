apiVersion: v1
kind: Service
metadata:
  name: sc-service-svc
  labels:
    run: sc-service
spec:
  type: NodePort
  ports:
  - port: 5000
    protocol: TCP
  selector:
    run: sc-service